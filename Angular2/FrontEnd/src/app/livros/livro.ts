export interface Livro {
        id: number;
        titulo: string;
        descricao: string;
        preco: number;
}

export interface LivroPost {
        id: number;
        titulo: string;
        descricao: string;
        preco: number;
}
